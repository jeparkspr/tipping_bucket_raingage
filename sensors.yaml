  # Rolling total rainfall sensors (statistics platform)
  - platform: statistics
    name: "Rainfall Total 5 min Raw"
    entity_id: sensor.raingage_rainfall_total
    state_characteristic: change
    max_age:
      minutes: 5
    sampling_size: 999
    precision: 2

  - platform: statistics
    name: "Rainfall Total 15 min Raw"
    entity_id: sensor.raingage_rainfall_total
    state_characteristic: change
    max_age:
      minutes: 15
    sampling_size: 999
    precision: 2

  - platform: statistics
    name: "Rainfall Total 30 min Raw"
    entity_id: sensor.raingage_rainfall_total
    state_characteristic: change
    max_age:
      minutes: 30
    sampling_size: 999
    precision: 2

  - platform: statistics
    name: "Rainfall Total 60 min Raw"
    entity_id: sensor.raingage_rainfall_total
    state_characteristic: change
    max_age:
      minutes: 60
    sampling_size: 999
    precision: 2